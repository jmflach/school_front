import classes from './Footer.module.css'

function Footer () {
    return <footer className={classes.footer}>
        <h3 className={classes.logo}>Desenvolvido por João Flach</h3>
    </footer>
}

export default Footer;